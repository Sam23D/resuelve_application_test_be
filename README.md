# ResuelveBe

To install the project make sure you have elixir ~> "1.7" installed, then run.
```
    mix deps.get
    mix compile
```
To run the program
```    
    iex -S mix
    iex> ResuelveBe.run
```

